<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.muslimroad">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MuslimRoad">
        <activity
            android:name=".ui.halalMain.HalalMainActivity"
            android:exported="true" />
        <activity
            android:name=".ui.chooseLanguages.ChooseLanguagesActivity"
            android:exported="true" />
        <activity
            android:name=".ui.chooseCountry.ChooseCountryActivity"
            android:exported="true" />
        <activity
            android:name=".ui.choose.ChooseActivity"
            android:exported="true" />
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>